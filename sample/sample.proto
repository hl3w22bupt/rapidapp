package rapidapp_sample;

message EchoBody {
    required uint64 seq = 1;
    optional string echo = 2;
}

message Mesg {
    required EchoBody body = 2;
}

service EchoService {
  rpc echo(Mesg) returns(Mesg);
}
